const c, t, p = require("@tools/classes").unpack()

return c({
	name = "ReplicatedStorage",
	description = "Provides a container for shared assets across the client and server.",
	since = "0.1.0",
})
