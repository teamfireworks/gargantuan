const c, t, p = require("@tools/classes").unpack()

return c({
	name = "DataModel",
	extends = "ServiceProvider",
	description = "Class that provides the root hierachy and services",
	since = "0.1.0",
})
