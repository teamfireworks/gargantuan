const c, t, p = require("@tools/classes").unpack()

return c({
	name = "GuiBase",
	description = "Abstract class for instances that compose graphical user interface",
	abstract = true,
	since = "0.1.0",
})
